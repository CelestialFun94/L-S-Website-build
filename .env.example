# Public Supabase configuration. The publishable key is safe for browser use
# only when every exposed table is protected by Row Level Security.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

